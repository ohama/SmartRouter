---
phase: 23-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/STATE.md
  - .planning/phases/23-documentation/23-01-SUMMARY.md
autonomous: true
plan_id: "23-01"
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "An operator reading README §10 can identify which Hermes flag (--pass-session-id) enables Tier 2 session continuity and choose among the four enablement options (direct CLI arg / shell alias / env var / wrapper script)."
    - "An operator reading README §10 understands that all three tiers (header / sysprompt / content fingerprint) are always active, run in a fixed fallback order, and have no config flag to disable any tier."
    - "An operator reading README §10 understands that stock Hermes with --pass-session-id fires Tier 2 (sysprompt parse), NOT Tier 1 (Tier 1 is for direct header-injection clients like curl)."
    - "README §10 contains no reference to FingerprintEnabled, PROXY-01, RemoteIp, network fingerprint, or HMRS-FUTURE-01 (all describe deleted v2.0 code)."
    - "README §7 Routing.Session does NOT contain a FingerprintEnabled row (already removed in commit 938c8ac); TtlMinutes and MaxEntries rows remain unchanged."
    - "README §8 documents the three Phase 22 session cascade counters (session_extraction_source_header, session_extraction_source_sysprompt, session_extraction_source_content) in both the example JSON block AND a description table that matches the existing Phase 19 block format."
    - "README §9.1 DecisionLog section is reviewed; no v2.1 schema changes were made, and (optionally) the cosmetic 'Phase 17–19' phase-range mention is bumped to 'Phase 17–22' for accuracy."
  artifacts:
    - path: "README.md"
      provides: "v2.1-accurate operator documentation for the three-tier session cascade, counters, and config"
      contains: "three-tier"
    - path: ".planning/phases/23-documentation/23-01-SUMMARY.md"
      provides: "Plan close-out with verification grep results, DOC-01..DOC-04 status, and §9.1 judgment-call outcome"
    - path: ".planning/STATE.md"
      provides: "Phase 23 marked complete; v2.1 milestone closed"
  key_links:
    - from: "README §10"
      to: "README §8 /stats counters"
      via: "Cross-reference: 'monitor which tier is active via /stats session_extraction_source_* counters'"
      pattern: "session_extraction_source"
    - from: "README §10"
      to: "README §5.6 sticky escalation"
      via: "Cross-reference: 'cascade resolves the session key that feeds sticky escalation (§5.6)'"
      pattern: "§5\\.6"
    - from: "README §10 Tier 2 description"
      to: "Hermes --pass-session-id behavior"
      via: "Explicit statement that --pass-session-id emits 'Session ID: <id>' in the system prompt, firing Tier 2"
      pattern: "--pass-session-id"
---

<objective>
Bring README.md into sync with v2.1 shipped behavior (Phase 22 closed the three-tier cascade
rewire). This is a pure documentation plan — no source code changes, no `dotnet build`, no
`dotnet test` runs. Verification is content-based (grep / wc -l / file presence).

Purpose: README is the operator-facing source of truth for this unattended launchd service.
After Phase 22, §10 still describes deleted v2.0 code (FingerprintEnabled, PROXY-01,
HMRS-FUTURE-01) and §8 omits the three new `/stats` cascade counters. Operators reading the
README today will be misled. CLAUDE.md mandates README sync for any change to §7, §8, §9.1, §10,
§11 — this plan satisfies that mandate for the v2.1 milestone.

Output:
- README §10 rewritten for v2.1 three-tier cascade (drops Fingerprint fallback / PROXY-01 block,
  documents Tier 1 / Tier 2 / Tier 3 semantics, lists the four `--pass-session-id` enablement
  options verbatim from the source doc, preserves the Hermes config jsonc block and the entire
  Graphify sub-section unchanged).
- README §8 gains three new field rows + JSON-example fields + a jq monitoring snippet for the
  Phase 22 cascade counters.
- README §7 verified (no edit — DOC-02 already complete in commit 938c8ac).
- README §9.1 reviewed; optional cosmetic phase-range bump if warranted.
- 23-01-SUMMARY.md + STATE.md updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-documentation/23-RESEARCH.md
@README.md
@.planning/phases/22-cascade-rewire-migration-and-observability/22-03-SUMMARY.md

# Source-of-truth files for content (read these to derive verbatim text):
@src/SmartRouter.Cli/Endpoints/Stats.fs
@src/SmartRouter.Cli/Adapters/SessionCascadeStats.fs
@src/SmartRouter.Cli/Endpoints/ChatCompletions.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify §7 (DOC-02) and review §9.1 (DOC-04)</name>
  <files>README.md (read-only for this task; potentially 1 small edit to §9.1)</files>
  <action>
    This task is the cheap, fast confirmation step. It is NOT a redo of DOC-02 — Plan 22-03
    commit `938c8ac` already removed the FingerprintEnabled row. This task VERIFIES that fact
    and decides on the §9.1 cosmetic phase-range bump.

    Step 1 — Verify §7 (DOC-02, idempotent check):
    - Read README.md lines 441–448 (the `### Routing.Session` block).
    - Confirm `FingerprintEnabled` is NOT present.
    - Confirm `TtlMinutes` and `MaxEntries` rows ARE present and unchanged.
    - Run: `grep -c 'FingerprintEnabled' README.md` — must return `0`.
    - If grep returns > 0, STOP and report a regression (do NOT continue; this would mean a
      prior commit re-introduced the row).

    Step 2 — Review §9.1 (DOC-04, judgment call):
    - Read README.md lines 636–668 (the `### 9.1 DecisionLog` section).
    - Confirm `schema_version` is still `1`, no `session_id` field added to the schema,
      `routing_reason` enum list unchanged from v2.0.
    - Line 659 currently reads: "All v2.0 additions (Phase 17–19) are additive enum values..."
    - JUDGMENT CALL: bump "Phase 17–19" → "Phase 17–22" for cosmetic accuracy.
      Recommendation from 23-RESEARCH.md §Open Questions Q1: do the bump (low cost,
      improves accuracy). Replace the single phrase `(Phase 17–19)` with `(Phase 17–22)` on
      line 659. Do NOT change any other wording in §9.1.
    - If the line 659 wording differs from the research's quoted text (e.g., already updated
      by a later commit), skip the edit and note this in the SUMMARY.

    Step 3 — Atomic commit (only if §9.1 was actually edited):
    - Stage README.md specifically (not `git add .`).
    - Commit message: `docs(23-01): verify §7 doc-02 idempotent; bump §9.1 phase-range to 17–22`
    - Note: if §9.1 was NOT edited (e.g., already up to date), skip the commit for this task
      and record the no-op verification in the SUMMARY instead.
  </action>
  <verify>
    - `grep -c 'FingerprintEnabled' README.md` returns `0`.
    - `grep -n 'TtlMinutes' README.md` returns at least one match in the §7 Routing.Session block (lines 441–448 range).
    - `grep -n 'MaxEntries' README.md` returns at least one match in the §7 Routing.Session block.
    - If the §9.1 cosmetic bump was applied: `grep -c 'Phase 17–22' README.md` returns at least 1; `grep -c 'Phase 17–19' README.md` returns 0.
    - If §9.1 was NOT edited (already correct or skip-decision): document the rationale in SUMMARY.
  </verify>
  <done>
    DOC-02 verified intact (zero `FingerprintEnabled` matches). §9.1 either updated for cosmetic accuracy or explicitly noted as not-needed in the SUMMARY. No regression introduced to §7 or §9.1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README §10 Hermes Integration for v2.1 three-tier cascade (DOC-01)</name>
  <files>README.md</files>
  <action>
    This is the largest task. Rewrite README §10 lines 781–875 to describe v2.1 shipped behavior.

    SCOPE BOUNDARIES (read 23-RESEARCH.md §Current State of README Sections for line-by-line
    audit):
    - Lines 781–789 (intro + `### Hermes Agent (v2.0 session-aware selfrouting)` heading) → REPLACE
    - Lines 791–800 (`#### Hermes config (unchanged from v1.x)` jsonc block) → PRESERVE UNCHANGED
    - Lines 801–820 (`#### X-Session-Id header opt-in (session continuity)`) → REPLACE with new
      three-tier cascade description + four enablement options
    - Lines 822–849 (`#### Fingerprint fallback (opt-in; loopback single-client only)`) → DELETE
      ENTIRELY (every line, including the PROXY-01 WARNING block at 831–837 and the
      FingerprintEnabled config note at 848)
    - Lines 851–875 (`### Graphify (concurrency-protected; sends task)`) → PRESERVE UNCHANGED

    NEW CONTENT TO INSERT (replace lines 781–820 and delete 822–849, keeping 791–800 + 851–875):

    1. Section heading: `### Hermes Agent (v2.1 — three-tier session cascade)`

    2. Intro paragraph (replaces lines 783–789) — describe the three-tier cascade in
       operator-facing prose. Must convey:
       - All three tiers always active; no config to disable any tier.
       - Tier order is fixed: header → sysprompt → content fingerprint.
       - Exactly one counter increments per request.
       - The cascade resolves the session key that feeds sticky escalation (§5.6).
       - Operators monitor tier distribution via `/stats session_extraction_source_*` counters
         (§8).

    3. Preserve `#### Hermes config (unchanged from v1.x)` block (lines 791–800) verbatim.

    4. New sub-section heading: `#### Session continuity — three-tier cascade`
       (or similar; this replaces the old `#### X-Session-Id header opt-in`).

       Body must describe each tier — derive wording from 23-RESEARCH.md §Three-Tier Cascade
       Authoritative Description (lines 174–207 of research). Required content per tier:

       - **Tier 1 — X-Session-Id HTTP header.** Fires when a client injects `X-Session-Id` as
         an HTTP header (curl, Graphify, custom tooling). NOT the channel used by stock Hermes
         `--pass-session-id`. Counter: `session_extraction_source_header`.
       - **Tier 2 — System-prompt `Session ID:` line.** Fires when the header is absent and the
         first system message contains a line matching `Session ID: <id>`. This IS the channel
         used by stock Hermes `--pass-session-id` (hermes-cli emits the id in the system prompt,
         not as an HTTP header). Counter: `session_extraction_source_sysprompt`.
       - **Tier 3 — Content fingerprint.** Always-on fallback: `SHA-256(system + "|||" + firstUser)`
         first 16 hex chars, lowercase. Fires when both Tier 1 and Tier 2 miss. Provides sticky
         continuity even without Hermes opt-in, but less precise than Tier 2 across context
         compression. Counter: `session_extraction_source_content`.

       Critical clarification (MUST be unambiguous in the prose): stock Hermes with
       `--pass-session-id` fires **Tier 2**, not Tier 1. Tier 1 is for direct-injection clients.
       This is from 23-RESEARCH.md §Common Pitfalls Pitfall 5.

    5. New sub-sub-section: `##### Enabling --pass-session-id (four options)`
       Quote VERBATIM the four options from 23-RESEARCH.md §Four `--pass-session-id` Enablement
       Options (lines 215–249 of research). Each option must include its labeled trade-off note:
       - Option 1: Direct CLI argument ("Simple but requires typing the flag every session.")
       - Option 2: Shell alias (recommended) ("Run `hermes-router` instead of `hermes`. Never forget the flag.")
       - Option 3: Environment variable (cite `hermes_cli/main.py:1276` reads
         `HERMES_TUI_PASS_SESSION_ID` and auto-enables the option; set in `~/.zshrc` for
         persistence.)
       - Option 4: Wrapper script (most robust) ("Works for multiple users; cannot be forgotten.")

       Each option must be in a `bash` code block matching the syntax in research lines 222–248.

       Add an operator note at the end of the enablement sub-section: without
       `--pass-session-id`, Tier 2 never fires and requests fall through to Tier 3 (content
       fingerprint). Tier 3 is less precise for context-compression scenarios.

    6. PRESERVE `### Graphify (concurrency-protected; sends task)` block (lines 851–875)
       verbatim. Do NOT touch this sub-section.

    REMOVAL VERIFICATION (after edit):
    - No mention of `FingerprintEnabled` anywhere in the new §10.
    - No mention of `PROXY-01`.
    - No mention of `RemoteIp` or `RemoteIpAddress`.
    - No mention of `HMRS-FUTURE-01`.
    - No "WARNING — NOT SAFE BEHIND REVERSE PROXIES" block.
    - No "future work" framing for the X-Session-Id header.
    - The smoke test reference (`./scripts/smoke-hermes-session.sh`, currently at lines 818–820)
      may be kept or omitted at the writer's discretion (research §Open Question Q3 says either
      is fine; the script was updated in commit `1842491` and is still accurate).

    Atomic commit:
    - Stage README.md specifically (not `git add .`).
    - Commit message: `docs(23-01): rewrite §10 for v2.1 three-tier session cascade`
  </action>
  <verify>
    Content-based verification (no `dotnet test` since no source change):

    - `grep -c 'FingerprintEnabled' README.md` returns `0`.
    - `grep -c 'PROXY-01' README.md` returns `0`.
    - `grep -c 'RemoteIp' README.md` returns `0`.
    - `grep -c 'HMRS-FUTURE-01' README.md` returns `0`.
    - `grep -c 'NOT SAFE BEHIND REVERSE PROXIES' README.md` returns `0`.
    - `grep -c 'three-tier' README.md` returns at least `1` (preferably 2+ — heading + intro).
    - `grep -c 'pass-session-id' README.md` returns at least `5` (one per enablement option + intro mention + Tier 2 description).
    - `grep -c 'HERMES_TUI_PASS_SESSION_ID' README.md` returns at least `1` (Option 3 env var).
    - `grep -c 'hermes-router' README.md` returns at least `1` (Option 2 shell alias).
    - `grep -c 'Session ID:' README.md` returns at least `1` (Tier 2 description).
    - `grep -c 'session_extraction_source_header' README.md` returns at least `1` (Tier 1 counter mention).
    - `grep -c 'session_extraction_source_sysprompt' README.md` returns at least `1` (Tier 2 counter mention; spelling: NO underscore between sys and prompt).
    - `grep -c 'session_extraction_source_content' README.md` returns at least `1` (Tier 3 counter mention).
    - `grep -n 'Hermes config' README.md` returns the preserved heading from line ~791.
    - `grep -n '### Graphify' README.md` returns the preserved Graphify heading from line ~851.
    - `wc -l README.md` — new line count should be roughly current minus ~30 lines (the deleted
      Fingerprint fallback sub-section is ~28 lines; the new Tier 1/2/3 + enablement options
      content is similar in length; net change should be near-zero or slightly negative).
  </verify>
  <done>
    README §10 reads as v2.1-accurate. An operator can identify which `--pass-session-id`
    option to use and understand that stock Hermes uses Tier 2 (sysprompt), not Tier 1.
    All five deleted-code markers (FingerprintEnabled, PROXY-01, RemoteIp, HMRS-FUTURE-01,
    "NOT SAFE BEHIND REVERSE PROXIES") are absent. The Hermes config block and Graphify
    sub-section are preserved verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 22 cascade counter rows to README §8 /stats (DOC-03)</name>
  <files>README.md</files>
  <action>
    Add the three Phase 22 cascade counters to README §8. Two insertion points:

    Insertion Point A — Stats JSON example (current lines ~570–580, ends at `selfrouter_skipped`):
    Append three new fields at the end of the JSON object so operators can see the shape.
    Match the existing JSON formatting (indentation, comma placement). Use realistic example
    values (e.g., low-but-nonzero counts), and ensure the LAST field has no trailing comma so
    the JSON stays valid.

    Insertion Point B — Counter description tables (immediately after the Phase 19 block at
    lines ~607–620, before the `curl` example or before the next sub-section):

    Insert this new block VERBATIM from 23-RESEARCH.md §§8 lines 140–149:

    ```markdown
    **Phase 22 — session cascade counters** (all `int64`, process-lifetime; exactly one counter
    increments per request regardless of `Routing.Mode`):

    | Field | Description |
    |---|---|
    | `session_extraction_source_header` | Requests where `X-Session-Id` HTTP header was present and non-empty (Tier 1). Operator-initiated via `hermes --pass-session-id` or direct header injection. |
    | `session_extraction_source_sysprompt` | Requests where header was absent but a `Session ID: <id>` line was found in the first system message (Tier 2). Indicates Hermes `--pass-session-id` is active. |
    | `session_extraction_source_content` | Requests that fell through to SHA-256 content fingerprint — `SHA-256(system + "|||" + firstUser)[..15]` (Tier 3). Indicates Hermes is running without `--pass-session-id`. |
    ```

    Correction note on Tier 1 wording: the research table says "Operator-initiated via
    `hermes --pass-session-id` or direct header injection." But the research itself
    (§Common Pitfalls Pitfall 5) clarifies that stock `hermes --pass-session-id` fires Tier 2,
    NOT Tier 1. Replace the Tier 1 description to read: "Requests where `X-Session-Id` HTTP
    header was present and non-empty (Tier 1). Set by direct-injection clients (curl,
    Graphify, custom tooling) — NOT by stock Hermes `--pass-session-id` (which fires Tier 2)."

    This keeps the §8 table semantically correct and avoids contradicting §10.

    Insertion Point C (optional but recommended) — Append a `jq` monitoring snippet after the
    new table block, following the existing Phase 19 `curl` snippet pattern. Suggested:

    ```bash
    # Monitor session cascade tier distribution (Phase 22)
    curl -s http://localhost:4000/stats | jq '{
      header: .session_extraction_source_header,
      sysprompt: .session_extraction_source_sysprompt,
      content: .session_extraction_source_content
    }'
    ```

    Exact field name spelling — CRITICAL (research §Common Pitfalls Pitfall 2):
    - `session_extraction_source_header` ✓
    - `session_extraction_source_sysprompt` ✓ (NO underscore between sys and prompt; double-p)
    - `session_extraction_source_content` ✓

    Do NOT write `_sys_prompt`, `_system_prompt`, or any variant. These names must match
    `StatsWire` record in `src/SmartRouter.Cli/Endpoints/Stats.fs` lines 51–53 byte-for-byte.

    Atomic commit:
    - Stage README.md specifically.
    - Commit message: `docs(23-01): add Phase 22 cascade counter rows to §8 /stats`
  </action>
  <verify>
    - `grep -c 'session_extraction_source_header' README.md` returns at least `3` (Tier 1 mention in §10, table row in §8, JSON example in §8; possibly jq snippet too).
    - `grep -c 'session_extraction_source_sysprompt' README.md` returns at least `3` (same breakdown; verify spelling has no underscore between sys and prompt).
    - `grep -c 'session_extraction_source_content' README.md` returns at least `3`.
    - `grep -c 'session_extraction_source_sys_prompt' README.md` returns `0` (wrong spelling guard).
    - `grep -c 'session_extraction_source_system_prompt' README.md` returns `0` (wrong spelling guard).
    - `grep -c 'Phase 22 — session cascade counters' README.md` returns at least `1`.
    - The JSON example block in §8 still parses as valid JSON (visual inspection — last field has no trailing comma; object is closed properly).
  </verify>
  <done>
    README §8 documents all three Phase 22 cascade counters in (a) the example JSON and
    (b) a description table matching the Phase 19 format. Field names match `StatsWire`
    byte-for-byte (verified by absence of wrong-spelling variants). Tier 1 row text
    correctly clarifies that stock `--pass-session-id` fires Tier 2, not Tier 1.
  </done>
</task>

<task type="auto">
  <name>Task 4: Cross-cutting drift sweep + SUMMARY + STATE update</name>
  <files>
    README.md (potential touch-up only),
    .planning/phases/23-documentation/23-01-SUMMARY.md (create),
    .planning/STATE.md (update)
  </files>
  <action>
    Step 1 — Cross-cutting README drift sweep (research §Cross-Cutting README Drift Inventory):

    Run the full grep suite across the entire README (not just §10) to catch any stragglers
    Tasks 2 and 3 may have missed:

    - `grep -n 'FingerprintEnabled' README.md` → expect 0 matches anywhere.
    - `grep -n 'network fingerprint' README.md` → expect 0 matches (this phrase was used in the
      deleted Fingerprint fallback sub-section; should not survive).
    - `grep -n 'RemoteIp' README.md` → expect 0 matches.
    - `grep -n 'PROXY-01' README.md` → expect 0 matches.
    - `grep -n 'HMRS-FUTURE' README.md` → expect 0 matches.
    - `grep -n 'v2.0 ships smart-router-side' README.md` → expect 0 matches.

    If any match is found outside the rewrites already made, edit the README to remove or
    update that mention. (Research expects no stragglers outside §10, but the grep is the
    sweep to be sure.)

    Step 2 — Write 23-01-SUMMARY.md:

    Create `.planning/phases/23-documentation/23-01-SUMMARY.md` with:
    - YAML frontmatter (status: complete, requirements_covered: [DOC-01, DOC-02, DOC-03, DOC-04],
      commits: [list of commit short SHAs from Tasks 1–3], affects: ["README.md", "operator-docs"],
      subsystem: "documentation")
    - Brief narrative: what was changed in §10 (rewrite scope), what was added to §8 (three
      counter rows + JSON example fields + jq snippet), what was verified in §7 (no edit
      needed), what was reviewed in §9.1 (cosmetic bump if applied, else explicitly noted as
      no-op).
    - Verification grep results (the full grep suite outputs from Task 4 Step 1).
    - DOC-01..DOC-04 status: PASS for each, with one-sentence evidence.
    - Open items: none expected; if §9.1 was deferred for any reason, note it here.

    Step 3 — Update `.planning/STATE.md`:
    - Mark Phase 23 as complete (set status field or update the milestone progress line per
      existing STATE.md conventions).
    - Close out v2.1 Hermes-less Session Tiering milestone (Phase 23 is the final phase per
      ROADMAP.md lines 74–92).
    - Append a one-line entry to the activity log noting the README sync.

    Step 4 — Atomic commit:
    - Stage `.planning/phases/23-documentation/23-01-SUMMARY.md`, `.planning/STATE.md`, and
      README.md (if Step 1 found stragglers requiring edits).
    - Commit message: `docs(23-01): close phase 23 (summary + state + drift sweep)`
  </action>
  <verify>
    - `test -f .planning/phases/23-documentation/23-01-SUMMARY.md` returns success.
    - `grep -q 'status: complete' .planning/phases/23-documentation/23-01-SUMMARY.md` (or
      whichever frontmatter convention the project uses for completed plan summaries — check
      a prior SUMMARY.md like `.planning/phases/22-cascade-rewire-migration-and-observability/22-03-SUMMARY.md` for the exact key).
    - Cross-cutting drift grep suite (Step 1) all return 0 matches.
    - `.planning/STATE.md` references Phase 23 as complete (visual inspection — exact wording
      depends on the project's STATE.md format).
    - Final commit count for this plan: 3–4 commits (Task 1 may have 0 commits if no §9.1
      edit; Tasks 2, 3, 4 each have 1 commit).
  </verify>
  <done>
    Phase 23 closed. SUMMARY.md captures the work, all drift grep checks pass, STATE.md
    reflects Phase 23 completion. v2.1 milestone documentation is sync'd to shipped behavior.
  </done>
</task>

</tasks>

<verification>

Phase 23 verification is fully content-based (no `dotnet build`, no `dotnet test` since no source
code change). The gsd-verifier should run this exact grep suite:

```bash
# Deleted-code markers — all must return 0 (DOC-01 success):
grep -c 'FingerprintEnabled' README.md          # expect 0
grep -c 'PROXY-01' README.md                    # expect 0
grep -c 'RemoteIp' README.md                    # expect 0
grep -c 'HMRS-FUTURE-01' README.md              # expect 0
grep -c 'HMRS-FUTURE' README.md                 # expect 0
grep -c 'NOT SAFE BEHIND REVERSE PROXIES' README.md  # expect 0
grep -c 'network fingerprint' README.md         # expect 0
grep -c 'v2.0 ships smart-router-side' README.md     # expect 0

# Required v2.1 content — all must return >0 (DOC-01 + DOC-03 success):
grep -c 'three-tier' README.md                              # expect >=1
grep -c 'pass-session-id' README.md                         # expect >=4
grep -c 'HERMES_TUI_PASS_SESSION_ID' README.md              # expect >=1
grep -c 'hermes-router' README.md                           # expect >=1
grep -c 'Session ID:' README.md                             # expect >=1
grep -c 'session_extraction_source_header' README.md        # expect >=3
grep -c 'session_extraction_source_sysprompt' README.md     # expect >=3
grep -c 'session_extraction_source_content' README.md       # expect >=3
grep -c 'Phase 22 — session cascade counters' README.md     # expect >=1

# Wrong-spelling guards — must return 0 (DOC-03 quality):
grep -c 'session_extraction_source_sys_prompt' README.md       # expect 0
grep -c 'session_extraction_source_system_prompt' README.md    # expect 0

# §7 idempotency check (DOC-02 still done):
grep -c 'TtlMinutes' README.md          # expect >=1 (preserved)
grep -c 'MaxEntries' README.md          # expect >=1 (preserved)
# (FingerprintEnabled already covered above — must be 0)

# §9.1 cosmetic bump (DOC-04 — only if Task 1 applied it):
# (No hard assertion; SUMMARY.md should document the decision either way.)

# Preserved sub-sections:
grep -c 'Hermes config' README.md       # expect >=1 (unchanged jsonc block preserved)
grep -c '### Graphify' README.md        # expect >=1 (Graphify sub-section preserved)

# Artifact existence:
test -f .planning/phases/23-documentation/23-01-SUMMARY.md
test -f .planning/STATE.md
```

If every grep above returns the expected count, Phase 23 is VERIFIED.

</verification>

<success_criteria>

Phase 23 is complete when:

1. **DOC-01 (§10 rewrite):** README §10 describes the v2.1 three-tier cascade with all three
   tiers (header / sysprompt / content fingerprint) explained from an operator perspective;
   the four `--pass-session-id` enablement options are documented verbatim per the source doc;
   the deleted Fingerprint fallback sub-section is gone (zero matches for FingerprintEnabled,
   PROXY-01, RemoteIp, HMRS-FUTURE-01, "NOT SAFE BEHIND REVERSE PROXIES" in the entire README);
   the Hermes config jsonc block and the Graphify sub-section are preserved unchanged.

2. **DOC-02 (§7 verification):** `grep -c 'FingerprintEnabled' README.md` returns `0` (already
   removed in commit 938c8ac; this plan VERIFIES, does not redo).

3. **DOC-03 (§8 counter rows):** README §8 documents the three Phase 22 cascade counters
   (`session_extraction_source_header`, `session_extraction_source_sysprompt`,
   `session_extraction_source_content`) with the exact spelling matching `StatsWire`. Both the
   JSON example and the description table show all three fields. A jq monitoring snippet is
   provided.

4. **DOC-04 (§9.1 review):** §9.1 reviewed; no v2.1 schema changes were needed. The cosmetic
   "Phase 17–19" → "Phase 17–22" bump is either applied (recommended) or explicitly noted in
   the SUMMARY as not-applied (with rationale).

5. **Closure:** 23-01-SUMMARY.md exists with frontmatter (status: complete, requirements:
   DOC-01..04), STATE.md reflects Phase 23 completion, atomic commits per task per CLAUDE.md
   format (`docs(23-01): <task-name>`).

</success_criteria>

<output>
After completion, ensure these files exist:
- `.planning/phases/23-documentation/23-01-SUMMARY.md` (created by Task 4)
- `.planning/STATE.md` (updated by Task 4)
- `README.md` (modified by Tasks 1–3; potentially Task 4 if drift sweep finds stragglers)

Expected commit count: 3 commits (Task 2, Task 3, Task 4) at minimum; 4 commits if Task 1
applies the §9.1 cosmetic bump.

Phase 23 closes the v2.1 Hermes-less Session Tiering milestone. Per ROADMAP.md lines 74–92,
Phase 23 is the final phase of v2.1; on completion, the milestone can be archived via
`/gsd:audit-milestone` and `/gsd:complete-milestone`.
</output>
